# Части разработки

Этот файл дублирует логику Excel-трекера в текстовом виде, чтобы архитектура проекта была понятна прямо в репозитории.

## Выполнено

- Часть 0: Подготовка гибридной архитектуры.

## Следующая часть

**Часть 1: Страница `configurator.html`**

Запрос для Codex:

```text
Реализуй часть 1: добавь configurator.html как отдельную страницу для React-конфигуратора.
```

Ожидаемый результат:

- создана отдельная HTML-страница;
- подключен общий дизайн `css/main.css`;
- добавлен контейнер `#configurator-root`;
- подготовлено место для будущего React bundle.

## Основное правило

Главная страница `index.html` остается статичной. React используется только для ядра конфигуратора.
